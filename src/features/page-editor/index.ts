/**
 * page-editor feature — public API.
 *
 * Responsible for the studio editing experience.
 * Depends on Redux store (page slice, auth slice).
 */
export { StudioToolbar } from '@components/studio/StudioToolbar';
export { SectionList } from '@components/studio/SectionList';
