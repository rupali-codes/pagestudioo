/**
 * page-viewer feature — public API.
 *
 * Responsible for rendering CMS-driven pages for end users.
 * No Redux dependency — purely data-in, UI-out.
 */
export { PageRenderer } from '@components/page/PageRenderer';
export { PageErrorBoundary } from '@components/page/PageErrorBoundary';
export { SectionRenderer } from '@components/page/SectionRenderer';
